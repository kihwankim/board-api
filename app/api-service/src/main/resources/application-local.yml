# local 환경
server:
  port: 8000

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

jwt:
  token:
    secret.key: JWTSecretKey
    expiration.seconds: 60480000
  http.request.header: Authorization